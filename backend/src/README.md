# src/
> 메인 앱 소스 (루트 레벨)

## 구조
```
app/
├── page.tsx      # 메인 페이지
├── layout.tsx    # 레이아웃
└── globals.css   # 전역 스타일

components/
└── Header.tsx    # 헤더
```

## 참고
- `web/` 폴더와 별도 존재
- 통합 필요 여부 검토 중
