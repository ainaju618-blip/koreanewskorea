# 🛡️ 범용 개발 원칙 v2.1 (Deepmind Style)

> "통제된 자율성(Controlled Autonomy)": 기존 스타일을 존중하되, 치명적 실수는 스스로 방어하라.

---

## 🚨 핵심 행동 강령 (Core Behavior)

1.  **100% 한국어 사고와 출력(Korean Native Process):**
    - **생각(Thought):** 문제를 분석하고 추론하는 **내부 독백(Chain of Thought) 과정을 반드시 한국어로 상세히 출력**하라.
    - **출력(Output):** 모든 답변, 설명, 보고서는 한국어로 작성한다. (단, 코드는 글로벌 표준 준수)
2.  **DNA 복제(Context Cloning):** 코드를 짜기 전, 반드시 **주변 파일(Reference)**을 읽고 그들의 스타일(네이밍, 구조)을 그대로 흉내 내라. (Global Rule보다 Local Context 우선)
3.  **능동적 해결(Active Fix):** 하드코딩, Any 타입 등 금지된 패턴이 필요해 보이면, 질문하지 말고 **올바른 패턴(상수화, 타입 정의)으로 즉시 변환**하여 구현하라.

---

## 1. UDP 6-Cycle 원칙 (생각의 순서)

작업 전, 내부적으로 이 6단계를 빠르게 시뮬레이션하고 **위험 요소(Risk)가 있을 때만 보고**하시오.

### Phase 0: DNA Extraction (청사진)
- **Reference:** 참고할 기존 파일은 무엇인가?
- **Style Sync:** 기존 코드의 에러 처리, 로깅 방식을 파악했는가?

### Phase 1: Context & Intent (의도)
- **Real Goal:** 사용자가 말한 기능의 '진짜 목적'은 무엇인가?

### Phase 2: Data Integrity (데이터 - 최우선)
- **Schema Safety:** 이 변경이 기존 DB 스키마나 FK 제약조건을 위반하지 않는가? (가장 치명적)
- **Migration check:** 마이그레이션이 필요한가?

### Phase 3: Logic & Pattern (로직)
- **DRY:** 기존 유틸리티/컴포넌트를 재사용하고 있는가?
- **Edge Cases:** 빈 값, 로딩 상태, 에러 상태 처리 확인.

### Phase 4: Security & Scalability (보안)
- **Basic RLS:** 최소한의 데이터 접근 제어(RLS)는 되어 있는가? (과도한 엔지니어링 금지)
- **Env Vars:** 비밀키는 환경변수로 관리되는가?

### Phase 5: Testability (검증)
- **Self-Correction:** 코드가 의도대로 돌지 않을 때, 로그를 보고 스스로 고칠 수 있는가?
- **MVP Validation:** 최소 기능 작동 확인 방법은?

---

## 2. ⚡ Auto-Fix Protocols (자동 보정)

다음 상황 발생 시 **중단하지 말고 아래 지침대로 즉시 수정**하시오.

1.  **하드코딩 감지** → `consts.ts`나 `.env`로 추출.
2.  **`any` 타입 필요** → 인터페이스 정의 또는 `unknown` + 타입 가드 사용.
3.  **복잡한 로직** → 함수/Hook으로 즉시 분리 및 주석 작성.

---

## 3. 🎯 작업 모드 (Workflow)

- **단순 작업:** [Phase 0(참조) → 코드 작성 → 검증] (보고 생략, 즉시 실행)
- **복잡한 작업:** [Phase 0~5 전체 체크 → **Decision Log(한 줄 계획)** 공유 → 실행]
    - *Decision Log 예시: "이 로직은 복잡하여 컴포넌트 내부에 두지 않고 `useBotStatus` 훅으로 분리하겠습니다."*

---

## 4. 📋 구체적 코딩 표준 (Context-Aware)

모든 파일 작성 시 Phase 0에서 파악한 **'현재 프로젝트의 기존 규칙'을 최우선**으로 적용한다.
- 네이밍 (camelCase vs snake_case)
- 임포트 순서 (라이브러리 -> 로컬)
- 컴포넌트 구조 (export default vs named export)

---
*이 규칙은 Antigravity Agent에 의해 2025-12-08 자동 생성 및 적용됨.*
