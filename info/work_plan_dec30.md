# 12월30일버젼 작업계획서

> **Version:** 1.1
> **Created:** 2025-12-30
> **Status:** PLANNING

---

## 1. 개요

### 1.1 목표
- 스크래핑과 AI 가공을 분리된 모듈로 관리
- **독립 폴더로 분리하여 별도 프로젝트로 구성**
- 관리자 페이지에서 두 기능을 통합 제어
- 단순하고 명확한 로컬 실행 환경

### 1.2 폴더 구조 (독립 프로젝트)

```
koreanews/
├── src/                    # 🛡️ PROTECTED (koreanewsone.com)
├── jeonnam/                # 지역 사이트 템플릿
├── koreanewshq/            # HQ 홈페이지
│
└── dec30-admin/            # 🆕 12월30일버젼 (독립 프로젝트)
    ├── package.json        # Port 3000 (공유)
    ├── next.config.ts
    ├── tsconfig.json
    │
    ├── scripts/            # Python 스크립트
    │   └── ai_only_processor.py
    │
    └── src/
        ├── app/
        │   ├── layout.tsx
        │   ├── page.tsx    # 메인 대시보드
        │   └── api/
        │       ├── scraper/route.ts
        │       └── ai-process/route.ts
        │
        ├── components/
        │   ├── ScraperControl.tsx
        │   ├── AIProcessControl.tsx
        │   └── LogViewer.tsx
        │
        └── lib/
            └── supabase.ts
```

### 1.3 현재 상태 분석

| 구분 | 파일 | 상태 | 문제점 |
|------|------|------|--------|
| 스크래핑 | 26개 개별 스크래퍼 | ✅ 정상 | 없음 |
| AI 가공 | `scripts/full_automation.py` | ⚠️ 혼합 | 스크래핑+AI 결합됨 |
| AI 프로세서 | `scripts/article_processor.py` | ✅ 정상 | 독립 사용 가능 |

### 1.4 목표 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│  dec30-admin (Port 3000)                                    │
│  독립 Next.js 앱 - "12월30일버젼"                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  [수동 스크래핑]              [AI 가공]                      │
│  ┌─────────────────┐         ┌─────────────────┐           │
│  │ 26개 지역 선택   │         │ 미가공 기사 조회 │           │
│  │ 실행 버튼       │         │ 일괄 가공 실행   │           │
│  │ 실행 로그       │         │ 진행 상황 표시   │           │
│  └─────────────────┘         └─────────────────┘           │
│                                                             │
└─────────────────────────────────────────────────────────────┘
        │                              │
        ▼                              ▼
┌───────────────────┐         ┌───────────────────────────┐
│ scrapers/         │         │ dec30-admin/scripts/      │
│ (기존 26개 유지)   │         │ ai_only_processor.py      │
└───────────────────┘         └───────────────────────────┘
        │                              │
        ▼                              ▼
┌─────────────────────────────────────────────────┐
│              Supabase (posts 테이블)             │
└─────────────────────────────────────────────────┘
```

---

## 2. 작업 목록

### Phase 1: 프로젝트 초기화

| # | 작업 | 파일 | 우선순위 |
|:-:|------|------|:--------:|
| 1.1 | dec30-admin 폴더 생성 | `dec30-admin/` | P0 |
| 1.2 | package.json 생성 (Port 3000) | `dec30-admin/package.json` | P0 |
| 1.3 | Next.js 설정 파일들 | `next.config.ts`, `tsconfig.json`, etc. | P0 |
| 1.4 | Tailwind 설정 | `tailwind.config.ts`, `globals.css` | P0 |

### Phase 2: 백엔드 구성

| # | 작업 | 파일 | 우선순위 |
|:-:|------|------|:--------:|
| 2.1 | AI 전용 가공 스크립트 | `dec30-admin/scripts/ai_only_processor.py` | P0 |
| 2.2 | Supabase 클라이언트 | `dec30-admin/src/lib/supabase.ts` | P0 |
| 2.3 | 스크래핑 API | `dec30-admin/src/app/api/scraper/route.ts` | P0 |
| 2.4 | AI 가공 API | `dec30-admin/src/app/api/ai-process/route.ts` | P0 |

### Phase 3: 프론트엔드 구성

| # | 작업 | 파일 | 우선순위 |
|:-:|------|------|:--------:|
| 3.1 | 레이아웃 | `dec30-admin/src/app/layout.tsx` | P0 |
| 3.2 | 메인 대시보드 | `dec30-admin/src/app/page.tsx` | P0 |
| 3.3 | 스크래핑 컨트롤 컴포넌트 | `dec30-admin/src/components/ScraperControl.tsx` | P1 |
| 3.4 | AI 가공 컨트롤 컴포넌트 | `dec30-admin/src/components/AIProcessControl.tsx` | P1 |
| 3.5 | 로그 뷰어 컴포넌트 | `dec30-admin/src/components/LogViewer.tsx` | P1 |

### Phase 4: 테스트 및 검증

| # | 작업 | 설명 | 우선순위 |
|:-:|------|------|:--------:|
| 4.1 | 로컬 서버 실행 테스트 | Port 3000에서 실행 확인 | P0 |
| 4.2 | 스크래핑 테스트 | 1개 지역 테스트 실행 | P0 |
| 4.3 | AI 가공 테스트 | Ollama qwen3:14b 연동 테스트 | P0 |
| 4.4 | 통합 테스트 | 전체 플로우 테스트 | P1 |

---

## 3. 상세 설계

### 3.1 AI 전용 가공 스크립트 (`scripts/ai_only_processor.py`)

**기능:**
- Supabase에서 미가공 기사 조회 (ai_processed = false)
- Ollama qwen3:14b로 3단계 AI 가공
- 가공 결과 Supabase 업데이트

**핵심 로직 (article_processor.py 활용):**
```python
# 기존 ArticleProcessor 클래스 재사용
from article_processor import ArticleProcessor

class AIOnlyProcessor:
    def __init__(self):
        self.processor = ArticleProcessor(model="qwen3:14b")
        self.supabase = create_client(SUPABASE_URL, SUPABASE_KEY)

    def get_unprocessed_articles(self, limit=10):
        """미가공 기사 조회"""
        return self.supabase.table('posts') \
            .select('*') \
            .eq('ai_processed', False) \
            .limit(limit) \
            .execute()

    def process_article(self, article):
        """단일 기사 AI 가공"""
        result = self.processor.process(article['content'])
        # 3단계: Convert → Refine → Verify
        return result

    def run(self, limit=10):
        """일괄 실행"""
        articles = self.get_unprocessed_articles(limit)
        for article in articles.data:
            processed = self.process_article(article)
            self.update_article(article['id'], processed)
```

### 3.2 API 엔드포인트

**AI 가공 API (`/api/bot/ai-process`):**
```typescript
// POST /api/bot/ai-process
// Body: { limit?: number, articleIds?: string[] }
// Response: { success: boolean, processed: number, errors: string[] }
```

**스크래핑 API (`/api/bot/run-scraper`):**
```typescript
// POST /api/bot/run-scraper
// Body: { regions: string[] }
// Response: { success: boolean, results: RegionResult[] }
```

### 3.3 관리자 페이지 UI 설계

```
┌─────────────────────────────────────────────────────────────────┐
│  12월30일버젼 - 수동 제어 패널                                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 📰 수동 스크래핑                                          │  │
│  ├──────────────────────────────────────────────────────────┤  │
│  │ 지역 선택:                                                │  │
│  │ ☑ 전체 선택  ☐ 광역시/도  ☐ 시  ☐ 군  ☐ 교육청          │  │
│  │                                                          │  │
│  │ ┌────────────────────────────────────────────────────┐   │  │
│  │ │ ☑ 광주  ☑ 전남  ☑ 목포  ☑ 여수  ☑ 순천  ...       │   │  │
│  │ │ ☑ 나주  ☑ 광양  ☑ 담양  ☑ 곡성  ☑ 구례  ...       │   │  │
│  │ └────────────────────────────────────────────────────┘   │  │
│  │                                                          │  │
│  │ [🚀 스크래핑 실행]  선택됨: 26개 지역                     │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 🤖 AI 가공                                                │  │
│  ├──────────────────────────────────────────────────────────┤  │
│  │ 미가공 기사: 47건                                         │  │
│  │ Ollama 모델: qwen3:14b                                    │  │
│  │ 처리 개수: [10 ▼]                                         │  │
│  │                                                          │  │
│  │ [🔄 AI 가공 실행]                                         │  │
│  │                                                          │  │
│  │ 진행 상황: ████████░░ 8/10 (80%)                         │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 📋 실행 로그                                              │  │
│  ├──────────────────────────────────────────────────────────┤  │
│  │ [12:30:15] 광주 스크래핑 시작...                          │  │
│  │ [12:30:18] 광주 완료: 5건 수집                            │  │
│  │ [12:30:20] 전남 스크래핑 시작...                          │  │
│  │ [12:30:25] 전남 완료: 8건 수집                            │  │
│  │ ...                                                      │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 4. 기술 스택

| 구분 | 기술 | 버전 |
|------|------|------|
| Frontend | Next.js + React | 16 / 19 |
| Backend API | Next.js API Routes | - |
| AI 모델 | Ollama qwen3:14b | - |
| 데이터베이스 | Supabase | - |
| 스크래퍼 | Python + Playwright | 3.x |

---

## 5. 실행 계획

### Day 1 (오늘)

| 순서 | 작업 | 상세 |
|:----:|------|------|
| 1 | 프로젝트 초기화 | `dec30-admin/` 폴더 + package.json + 설정 파일들 |
| 2 | AI 가공 스크립트 | `scripts/ai_only_processor.py` |
| 3 | Supabase 클라이언트 | `lib/supabase.ts` |
| 4 | API 엔드포인트 | `api/scraper/`, `api/ai-process/` |
| 5 | 프론트엔드 | 레이아웃 + 메인 페이지 + 컴포넌트 |
| 6 | 테스트 | Port 3000 실행 확인 |

**src/ 폴더 수정: 없음 (P0 보호)**

---

## 6. 파일 구조 (최종)

```
koreanews/
├── src/                          # 🛡️ PROTECTED (수정 금지)
├── jeonnam/                      # 지역 사이트
├── koreanewshq/                  # HQ 홈페이지
├── scrapers/                     # 기존 26개 스크래퍼 (그대로 사용)
│
└── dec30-admin/                  # 🆕 독립 프로젝트
    ├── package.json              # Port 3000 (공유)
    ├── next.config.ts
    ├── tsconfig.json
    ├── tailwind.config.ts
    ├── postcss.config.mjs
    │
    ├── scripts/
    │   └── ai_only_processor.py  # 🆕 AI 전용 가공 스크립트
    │
    └── src/
        ├── app/
        │   ├── globals.css
        │   ├── layout.tsx
        │   ├── page.tsx          # 메인 대시보드
        │   └── api/
        │       ├── scraper/
        │       │   └── route.ts  # 스크래핑 실행 API
        │       └── ai-process/
        │           └── route.ts  # AI 가공 API
        │
        ├── components/
        │   ├── ScraperControl.tsx
        │   ├── AIProcessControl.tsx
        │   └── LogViewer.tsx
        │
        └── lib/
            └── supabase.ts
```

---

## 7. 성공 기준

- [ ] 관리자 페이지에서 지역 선택 후 스크래핑 실행 가능
- [ ] 관리자 페이지에서 AI 가공 실행 가능
- [ ] 실행 로그 실시간 표시
- [ ] Ollama qwen3:14b 연동 정상 동작
- [ ] 에러 발생 시 명확한 에러 메시지 표시

---

## 8. 리스크 및 대응

| 리스크 | 영향 | 대응 방안 |
|--------|------|-----------|
| Ollama 서버 미실행 | AI 가공 실패 | 실행 전 상태 체크 + 가이드 표시 |
| 스크래핑 대상 사이트 구조 변경 | 데이터 수집 실패 | 에러 로깅 + 알림 |
| 동시 실행 충돌 | 중복 처리 | Lock 메커니즘 유지 |

---

*작업계획서 v1.0 - 2025-12-30*
