
// Scheduler disabled - manual execution only via Dec30 panel
export async function register() {
    // No automatic scheduling
    console.log('[Instrumentation] Scheduler disabled - use Dec30 panel for manual execution');
}
